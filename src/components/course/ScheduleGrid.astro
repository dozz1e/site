---
interface Props {
  schedule: Array<{
    eyebrow: string;
    title: string;
    body: string;
    options: Array<{ availability: string; title: string; items: string[] }>;
  }>;
}
const { schedule } = Astro.props;
---
{schedule.map((block) => (
  <section class="detail-section">
    <p class="eyebrow">{block.eyebrow}</p>
    <h2>{block.title}</h2>
    <p>{block.body}</p>
    <div class="schedule-grid">
      {block.options.map((option) => (
        <article>
          <span class="availability">{option.availability}</span>
          <h3>{option.title}</h3>
          <ul>
            {option.items.map((item) => <li>{item}</li>)}
          </ul>
        </article>
      ))}
    </div>
  </section>
))}
